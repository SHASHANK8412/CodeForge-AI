# Prometheus Configuration for AIForge Observability Platform
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'aiforge_fastapi'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['localhost:8000']
