[pytest]
testpaths = tests
norecursedirs = .venv node_modules frontend dist build .git
addopts = --import-mode=importlib
filterwarnings =
    ignore::DeprecationWarning:fastapi.testclient
    ignore::starlette.exceptions.StarletteDeprecationWarning
